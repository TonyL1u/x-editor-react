import styled from 'styled-components';

export default styled.div`
    position: relative;
    box-sizing: border-box;
    width: 100%;
    background: #fff;
    font-size: 14px;

    .iconTerminal {
        margin-right: 6px;
    }

    .iconBan {
        position: absolute;
        bottom: 8px;
        right: 8px;
        cursor: pointer;
    }
`;
